#!/bin/sh
set -eu

if command -v systemctl >/dev/null 2>&1; then
  systemctl stop specproof-station-host.service specproof-capture.service || true
  systemctl disable specproof-station-host.service specproof-capture.service || true
fi
